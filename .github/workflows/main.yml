name: A workflow for my Hello World file
on: push

jobs:
  build:
    name: Hello world action
    runs-on: ubuntu-latest    
    steps:
    - uses: actions/checkout@v1
    #- uses: ./action-a
    #  env:
    #    MY_NAME: "Mona"
    - uses: unosquare/release-npm-package@Initial-Version
      id: thismyid
      name: ReleasePackage
      with:
        release-version: Patch
