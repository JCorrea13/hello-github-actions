name: Release

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        with:
          repository: unosquare/release-npm-package
          token: ${{ secrets.GITHUBPAT }}
      - uses: JCorrea13/MyFirstAction@main
        id: thismyid2
        name: HelloActionWithDocker
        with:
          release-versiont: 'Patch'
